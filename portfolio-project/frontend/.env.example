# Optional: URL of the Spring Boot backend (see /backend).
# Leave unset to use a plain mailto: fallback for the contact form —
# the site works fully static without a backend.
VITE_API_URL=http://localhost:8080
